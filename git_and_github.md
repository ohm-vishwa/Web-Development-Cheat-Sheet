### Configuring Git
==}> see configuration
```sh
git config --list
```
==}> set user name
```sh
git config --global user.name "ohm_vishwa"
```
==}> set email
```sh
git config --global user.email "example@email.com"
```
